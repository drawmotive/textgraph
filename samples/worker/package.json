{
  "name": "textgraph-sample-worker",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "assets": "textgraph-copy-assets public/textgraph/wasm",
    "predev": "npm run assets",
    "dev": "vite --host 127.0.0.1",
    "prebuild": "npm run assets",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "@drawmotive/textgraph": "file:../.artifacts/textgraph.tgz"
  },
  "devDependencies": {
    "vite": "8.3.0"
  }
}
